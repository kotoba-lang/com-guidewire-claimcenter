{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([guidewire_claimcenter.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'guidewire_claimcenter.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
